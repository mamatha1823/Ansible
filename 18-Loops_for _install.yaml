- name: loops
  become: yes
  tasks:
    - name: install s/w with loops
      ansible.builtin.package:
        msg: "installing the {{item}}"
      loop:
        - postfix
        - ansible
        - nginx
  hosts: web1
