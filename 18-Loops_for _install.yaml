- name: loops
  become: yes
  hosts: web1
  tasks:
    - name: install s/w with loops
      ansible.builtin.package:
        name: "{{item}}""
        state: present
      loop:
      - postfix
      - ansible
      - nginx
  
